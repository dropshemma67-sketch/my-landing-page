<section class="section">
  <div class="wrap" style="max-width:820px">
    <h1 class="h2 center" style="margin-bottom:2rem" data-reveal>{{ page.title }}</h1>
    <div class="rte glass" style="padding:clamp(1.5rem,3vw,2.4rem)" data-reveal>{{ page.content }}</div>
  </div>
</section>

{% schema %}
{ "name": "Página", "tag": "section", "settings": [] }
{% endschema %}
